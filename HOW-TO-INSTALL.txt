1) get the babelnet mvn project and install it in your ./m2 local maven repository
	1a) get babelnet
	1b) mvn validate
	1c) mvn install
	
2) mvn package

3) copy the produce jar into the work directory

4) copy the directory config into the same working directory (it is used to configure babelnet)
